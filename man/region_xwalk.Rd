% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalks.R
\name{region_xwalk}
\alias{region_xwalk}
\title{Region Crosswalk}
\source{
Colorado State Demography Office, \code{econ.crosswalk_region}
}
\usage{
region_xwalk(con = NULL)
}
\arguments{
\item{con}{Optional open database connection, as returned by
\code{\link[sdotools:sdo_db_connect]{sdotools::sdo_db_connect()}}. Supply one to reuse a connection across
several calls; otherwise a short-lived read-only connection is opened.}
}
\value{
A tibble with 159 rows and 4 variables:
\describe{
\item{region_code}{Region code}
\item{region_name}{Region name}
\item{county_fips}{County FIPS code, 3 digits}
\item{county_name}{County name}
}
}
\description{
Colorado counties mapped to regions. Counties appear once per region they
belong to, so the table is longer than the county count.
}
\details{
Read from \code{econ.crosswalk_region} in the SDO Postgres database. The database
table also carries \code{region} and \code{countyfips} columns; these are dropped here
to preserve the shape of the dataset this function replaced.
}
\examples{
\dontrun{
region_xwalk()
}
}
